this creates an new vagrant virtual machine & cookbook that installs nginx

1. create nginx recipe
2. create Vagrantfile
3. enable network in Vagrantfile, enable chef and add recipes in Vagrantfiles

